# Benchmarks

Benchmark runners and measured results are introduced after the semantic index
and MCP tools exist. Phase 0 records no performance or token-efficiency claims.

Raw measurements belong in `results/`; deterministic task definitions and
checkers belong in `tasks/`.
