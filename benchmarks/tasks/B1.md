# B1 — Lineage

Workbook: `tests/fixtures/generated/cross_sheet_model.xlsx`.

Enumerate the distinct input cells on the Inputs sheet that feed Summary!C10, directly or transitively.

Use only the configured workbook MCP tools. Do not use Python, shell commands, or another spreadsheet library. You may explain your reasoning briefly.

Expected JSON shape: `{"input_cells":["Sheet!A1",...]}`

Grading semantics: Array order is not significant to grading; duplicates are not accepted.

The last line of your reply must be exactly: `ANSWER: <json>`
