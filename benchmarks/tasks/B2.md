# B2 — Formula audit

Workbook: `tests/fixtures/generated/benchmarks/formula_blocks.xlsx`. This is the canonical task fixture plus the deterministic, disclosed archive workload from `benchmarks/workloads.py`.

Find every hand-edited formula cell that breaks the dominant copied-formula pattern.

Use only the configured workbook MCP tools. Do not use Python, shell commands, or another spreadsheet library. You may explain your reasoning briefly.

Expected JSON shape: `{"tampered_cells":["Sheet!A1",...]}`

Grading semantics: Array order is not significant to grading; duplicates are not accepted.

The last line of your reply must be exactly: `ANSWER: <json>`
