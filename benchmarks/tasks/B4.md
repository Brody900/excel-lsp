# B4 — Schema

Workbook: `tests/fixtures/generated/benchmarks/mixed_types.xlsx`. This is the canonical task fixture plus the deterministic, disclosed archive workload from `benchmarks/workloads.py`.

Report the ordered column names and inferred dtypes for the MixedTypes table.

Use only the configured workbook MCP tools. Do not use Python, shell commands, or another spreadsheet library. You may explain your reasoning briefly.

Expected JSON shape: `{"columns":[{"name":"Column","dtype":"str"},...]}`

Grading semantics: Array order and all scalar values are significant to grading.

The last line of your reply must be exactly: `ANSWER: <json>`
