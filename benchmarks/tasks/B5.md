# B5 — Impact

Workbook: `tests/fixtures/generated/benchmarks/cross_sheet_model.xlsx`. This is the canonical task fixture plus the deterministic, disclosed archive workload from `benchmarks/workloads.py`.

If Inputs!B2 changes, report the conservative dependent formula ranges returned by semantic block-level impact analysis. De-duplicate ranges.

Use only the configured workbook MCP tools. Do not use Python, shell commands, or another spreadsheet library. You may explain your reasoning briefly.

Expected JSON shape: `{"dependent_ranges":["Sheet!A1:A2",...]}`

Grading semantics: Array order is not significant to grading; duplicates are not accepted.

The last line of your reply must be exactly: `ANSWER: <json>`
