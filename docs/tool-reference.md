# Tool reference

No workbook or MCP tools are implemented in Phase 0. The current command-line
surface is limited to `excel-lsp --help` and `excel-lsp --version`.

This document will describe exact tool schemas, caps, pagination, and error
contracts when the server lands in Phase 7.
