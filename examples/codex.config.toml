# Planned v0.1.0 Codex MCP configuration. The P9 clean-install gate must verify
# this exact command before the README removes its pre-release warning.
[mcp_servers.excel-lsp]
command = "uvx"
args = ["excel-lsp", "serve"]
