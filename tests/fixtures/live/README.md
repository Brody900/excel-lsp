# Live-authored Excel fixtures

`L1-model.xlsx`, `L2-table.xlsx`, and `L3-broken.xlsx` are authored and saved
with desktop Microsoft Excel during the required P8 protocol. They are retained
as native Excel output; deterministic generated fixtures remain under
`../generated/`.
